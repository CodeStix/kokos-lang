
// okey i didn't notice that was the problem, your solution doesn't quite work, see below:

type CString = unmanaged [Int8];

import function puts(str: CString);

// please add a new keyword that can be inferred or typed manually: readonly
function getStr() {
    return "hello world!"; 
}

struct Person {
    name: CString,
    age: Int
}

// static let person: Person?;

function createPerson(name: CString) {
    return Person(name: name, age: 200);
}

export function main() {

    let person = createPerson("stijn2");
    let person2 = createPerson("stijn3");

    puts(person.name);
    puts(person2.name);
    return person.age + person2.age;
}