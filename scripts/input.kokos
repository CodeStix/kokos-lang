
// okey i didn't notice that was the problem, your solution doesn't quite work, see below:

type CString = unmanaged [Int8];

import function puts(str: CString);

// please add a new keyword that can be inferred or typed manually: readonly
function getStr() {
    return "hello world!"; 
}

struct Person {
    age: Int
}

static let person: Person;

export function main() {
    return person.age;
}



// please implement the readonly modifier. readonly can be added to pointer types like non-value structs and non-value arrays and prevents being written to a pointed-to value