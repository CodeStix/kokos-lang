

struct Person { age: Int32 }

import function abs(n: Int32): Int32;

function readAge(p: unmanaged Person): Int32 {
    return p.age;
}

export function main(): Int32 {
    let ownedPerson = Person(age: 42);
    let a = readAge(ownedPerson);
    let b = abs(0 - 10);
    return a + b;
}